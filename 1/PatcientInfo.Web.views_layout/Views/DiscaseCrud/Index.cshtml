@model IEnumerable<PatcientInfo.Web.Models.Dicase.DicaseSelectionModel>
@{
    ViewBag.Title = "Index";
}

@*<h2>Index</h2>*@
<h3>Редагування даних про пацієнтів та хвороби</h3>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
    @Html.ActionLink("Додати хворобу ", "Create",
        null, new { @class = "btn btn-default" })
</p>




<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nazva)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DicaseTypeName)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.note)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @*@Html.DisplayFor(modelItem => item.Sorname)*@
                @Html.ActionLink(item.Nazva, "Details", new { id = item.Id })
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DicaseTypeName)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.note)
            </td>
            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id=item.Id })*@
                @Html.ActionLink("Редагувати", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Видалити", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
